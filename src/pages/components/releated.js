import React from 'react';
import imgJS from '../../../images/logos/videospace.png';
function Releated(props) {
    return (
        <div width={300}>
            <img width={300} height={100} src={imgJS}></img>
        </div>
    );
}

export default Releated;